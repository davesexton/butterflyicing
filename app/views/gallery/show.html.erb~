<div id="galleryNav">
  <%= link_to 'back to gallery', gallery_path() %>
</div>
<div id="galleryDisplay">
  <% @pics.each do |pic| %>
    <%= link_to(image_tag(pic['image_url']), pic['link']) %>
  <% end %>
</div>
<div></div>
